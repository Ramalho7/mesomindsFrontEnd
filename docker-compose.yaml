services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mesominds-frontend
    ports:
      - "3000:80"  
    restart: unless-stopped
    environment:
      - NODE_ENV=production