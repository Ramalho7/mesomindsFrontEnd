export type User = {
    id: number,
    nome: string,
    email: string,
    tipo: string,
    criador: string | null,
    ultimo_editor: string | null,
    status: string,
    created_at: string,
    updated_at: string, 
}